node_modules/
dist/
dist-node/
.turbo/
target/
src-tauri/target/
src-tauri/gen/
# Sidecar binaries are produced by scripts/build-sidecar.mjs per platform.
# Keep the directory around via .gitkeep but don't commit the blobs.
src-tauri/binaries/drawcast-mcp-*
.DS_Store
*.log
*.tsbuildinfo
pnpm-debug.log*
.claude/
evals/results/
# Per-loop scratch notes are throwaway, but _history.jsonl at the root
# of automation-runs/ must be tracked so each new run can read prior
# selections and apply the diversity rules in the loop prompt.
evals/automation-runs/*/
